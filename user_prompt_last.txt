<USER_REQUEST>
# CORRECTION BEFORE IMPLEMENTATION — POSITION DATA CONTRACT

Không được Proceed theo implementation plan hiện tại nguyên bản.

Sau khi review lại yêu cầu product, có một thay đổi kiến trúc bắt buộc:

## 1. POSITION DATA CONTRACT MỚI

User yêu cầu:

> Database phải giữ position thực tế từ provider. Frontend/Card/Filter mới thực hiện mapping sang position code viết tắt.

Do đó:

### DATABASE

Không ép:

```text
Defence → CB
Midfield → CM
Offence → ST
```

Không được làm mất source information.

`players.primary_position` phải giữ **position source/provider thực tế** nếu provider cung cấp.

Ví dụ:

```text
football-data.org:
Goalkeeper
Defence
Midfield
Offence

API-Football:
Goalkeeper
Right Back
Centre Back
Defensive Midfielder
Central Midfielder
Attacking Midfielder
Left Winger
Right Winger
Centre Forward
Striker
```

không được normalize các giá trị này thành `GK/CB/CM/ST` trước khi persistence.

---

# 2. KHÔNG THÊM `source_position` NẾU KHÔNG CẦN

Không cần thêm:

```text
source_position
```

nếu có thể sử dụng `primary_position` làm source position.

Không tạo thêm column chỉ để giải quyết vấn đề mà existing field đã có thể giải quyết.

Do đó trước tiên hãy kiểm tra schema/ORM hiện tại.

Nếu `primary_position` có thể lưu provider/source string:

```text
KEEP EXISTING COLUMN
```

không migration.

---

# 3. PRESENTATION POSITION PHẢI LÀ LAYER RIÊNG

Frontend có một utility duy nhất:

```text
toDisplayPosition()
```

Ví dụ:

```text
Goalkeeper          → GK
Defence             → DEF
Midfield            → MID
Offence             → FWD

Left Back           → LB
Centre Back         → CB
Right Back          → RB
Left Wing Back      → LWB
Right Wing Back     → RWB

Central Midfielder  → CM
Defensive Mi
<truncated 8936 bytes>
fake statistics
✅ No data reset
✅ No destructive DB cleanup
✅ Existing football-data.org sync still works
✅ API-Football enrichment still works
✅ Sportmonks preserved if still required
✅ Tests pass
✅ Build pass
✅ Lint pass
```

---

# 23. FINAL REPORT

Must explicitly show:

```text
POSITION CONTRACT

Database value:
<real provider value>

Display value:
<short code>

Example:
Right Back → RB
Defence → DEF
Midfield → MID
Offence → FWD
```

And:

```text
COMPETITION CLEANUP

Before:
Premier League records = X

After:
Premier League records = X

Deleted:
...
Reason:
...
```

And:

```text
API-FOOTBALL

Players enriched:
Photos:
Height:
Weight:
Preferred foot:
Shirt number:
Detailed positions:
Season statistics:
Match statistics:
Lineups:
Events:
```

And:

```text
TESTS
BUILD
LINT
DATABASE VERIFICATION
```

Không được báo "completed" nếu chưa có evidence.

---

# 24. FINAL ARCHITECTURE

Target:

```text
Provider
   ↓
REAL SOURCE POSITION
   ↓
PostgreSQL
   ↓
Backend API
   ↓
Presentation Position Mapper
   ↓
┌───────────────┬───────────────┬───────────────┐
Player Card     Filter          Compare/Radar
   ↓               ↓                  ↓
RB              RB                 RB
```

**SOURCE DATA phải trung thực.**

**DISPLAY DATA mới được viết tắt.**

**Không biến presentation requirement thành database transformation.**

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-03T15:00:00+07:00.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Claude Sonnet 4.6 (Thinking) to Gemini 3.8 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>